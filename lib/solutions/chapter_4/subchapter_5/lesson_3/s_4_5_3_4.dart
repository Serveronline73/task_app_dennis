import 'package:flutter/material.dart';

class S4534 extends StatelessWidget {
  const S4534({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const MyStatelessWidget34();
  }
}

class MyStatelessWidget34 extends StatelessWidget {
  const MyStatelessWidget34({super.key});
  @override
  Widget build(BuildContext context) {
    return const Row(
        mainAxisAlignment: MainAxisAlignment.spaceBetween,
        children: [
          Text("App"),
          SizedBox(width: 64),
          Text("Akademie"),
        ]);
  }
}
